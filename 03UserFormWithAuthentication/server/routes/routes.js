/*
import express from "express";
import { login, signup, user } from "../service/service";
const router = express.Router();

//Routes
router.post("/signup", signup);
router.post("/login", login);
router.get("/user", user);

export default router;
*/
